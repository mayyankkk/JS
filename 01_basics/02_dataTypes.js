"use strict"  // treat all JS code as newer version of JS standards(using only new syntaxes )

// alert("hello")  // we're using nodejs not browser

console.log("Mayank") // code readability should be high
console.log("js")

let name= "Mayank" // String
let age=18 //integer
let isLoggedIn=true //boolean

// Premitive datatypes


//number => limited range
// bigint=> (generally not sonsumed)
// string=> ""
// boolean=> true/false
// null=> standalone value(data type bhi hai) => representation of empty value (special type)
// undefined=> jb value defined nhi h (ex: datatype banaya pr uski value assign nhi ki hai )

//symbol=> used for  uniqueness, mostly used in react  and figma



console.log(typeof "mayank") // will print data type of provided input, in this case it's string
console.log(typeof age) //number
console.log(typeof null) // in this case type is given as object-> null object
console.log (typeof undefined) // in this case the type is undefined